#!/bin/bash
# Open the remote origin of a Git repository
remote=$(git config --get remote.origin.url)
repo=$(echo $remote | cut -c 16-)
url="https://github.com/$repo"
open $url &> /dev/null & disown $!
