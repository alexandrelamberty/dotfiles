#!/bin/bash

# Make an i3 window floating and set size and position
# !!! WORK ONLY for single monitor
# TODO: multiple monitor, rpc ? Move to i3
i3-msg floating enable, resize set 1150 px 660 px, move position 70 px 70 px
